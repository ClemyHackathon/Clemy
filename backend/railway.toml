[build]
builder = "nixpacks"
buildCommand = "./gradlew bootJar -x test"

[deploy]
startCommand = "java -jar build/libs/clemy-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/api/dashboard"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
